"""Perception PROVIDERS -- what a screen was read WITH, and how well.

Native only today. The package exists now so the seam that stamps
``origin``/``fidelity`` has one home before any toolkit-specific reader is
written; steps 5 and 6 add readers here without re-deciding the partition rule.
"""
